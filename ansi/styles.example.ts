import {
    bold,
    faint,
    italic,
    underline,
    blinking,
    inverse,
    hidden,
    strikethrough
} from './styles.ts'

console.log(`
    ${bold('Bold')}
    ${faint('Faint')}
    ${italic('Italic')}
    ${underline('Underline')}
    ${blinking('Blinking')}
    ${inverse('Inverse')}
    ${hidden('Hidden')}
    ${strikethrough('Strikethrough')}
`)
